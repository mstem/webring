# Password for the /admin page (view pending submissions). Required for /admin to work.
ADMIN_PASSWORD=

# Directory for runtime-writable data (submissions.json). Defaults to the repo root if unset.
# On Coolify this points at a mounted persistent volume so submissions survive redeploys.
DATA_DIR=
